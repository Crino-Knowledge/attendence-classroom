# 课堂考勤小程序 - 项目完整介绍

## 一、项目概述

这是一个基于微信小程序的课堂考勤系统，支持固定班级考勤、deviceId 设备绑定、距离验证等功能。

### 原始需求
- 班长发布口令，学生快速签到
- 支持面对面建群（距离验证）
- 辅导员、管理员、学生三种身份
- 设备绑定防作弊

---

## 二、技术架构

```
┌─────────────────────────────────────────────────┐
│                   微信小程序                      │
│                 miniprogram/                     │
│  ├── app.js           # 全局 API 调用            │
│  ├── utils/          # 工具函数                  │
│  │   ├── request.js  # 网络请求                  │
│  │   └── device.js   # 设备 ID 生成               │
│  └── pages/          # 页面                      │
└────────────────────┬────────────────────────────┘
                     │
                     │ HTTP 请求
                     ▼
┌─────────────────────────────────────────────────┐
│              后端服务（云托管/服务器）            │
│                 backend/                       │
│  ├── server.js      # Express 服务              │
│  └── data/         # JSON 数据存储              │
│      ├── counselors.json    # 辅导员账号        │
│      └── classes/          # 班级数据           │
│          └── class_xxx/    # 班级文件夹        │
│              ├── accounts.json   # 账号         │
│              ├── passcode.json  # 口令          │
│              └── attendance.json # 签到记录     │
└─────────────────────────────────────────────────┘
```

---

## 三、身份系统

| 身份 | 账号位置 | 权限 |
|------|----------|------|
| 辅导员 | `backend/data/counselors.json` | 设备绑定、创建/删除班级、增删改账号、发布口令 |
| 管理员（班长） | `backend/data/classes/xxx/accounts.json` | 设备绑定、发布口令、查看签到名单 |
| 学生 | `backend/data/classes/xxx/accounts.json` | 设备绑定、签到 |

### 初始账号

| 账号 | 密码 | 身份 | 班级 |
|------|------|------|------|
| counselor | counsel123 | 辅导员 | - |
| admin001 | admin123 | 管理员 | class_2024 |
| admin002 | admin123 | 管理员 | class_2024 |
| 2023001 | 123456 | 学生 | class_2024 |
| 2023002 | 123456 | 学生 | class_2024 |
| 2023003 | 123456 | 学生 | class_2024 |

### 示例班级（新增）

| 班级 | 账号 | 密码 | 姓名 |
|------|------|------|------|
| class_计算机2511 | 2024001 | 888888 | 赵六 |
| class_计算机2511 | 2024002 | 888888 | 钱七 |

---

## 四、核心功能逻辑

### 1. 设备绑定机制

- **永久设备绑定（deviceId）**：首次登录时自动绑定，之后无法在其他设备登录
- **当日设备绑定（todayDeviceId）**：每天 0 点自动清空，允许当天换设备（需口令验证）

```javascript
// 登录时检查设备
if (user.deviceId && user.deviceId !== deviceId) {
  return res.json({ success: false, msg: '账号已在其他设备登录' });
}

// 首次登录绑定设备
if (!user.deviceId) {
  user.deviceId = deviceId;
  saveClassAccounts(classId, accounts);
}
```

### 2. 定时重置

每天 0 点自动执行：
- 清空所有账号的 `todayDeviceId`
- 清空所有口令和签到记录

```javascript
cron.schedule('0 0 * * *', () => {
  // 重置班级账号
  // 重置辅导员设备
});
```

### 3. 距离验证

学生签到时，如果口令包含位置信息，验证距离在 100 米内：

```javascript
const distance = getDistance(lat1, lon1, lat2, lon2);
if (distance > 100) {
  return res.json({ success: false, msg: '距离太远，无法签到' });
}
```

---

## 五、文件结构

```
/workspace/
├── miniprogram/                    # 微信小程序
│   ├── app.js                     # 全局配置和 API
│   ├── app.json                   # 小程序配置
│   ├── app.wxss                   # 全局样式
│   ├── utils/
│   │   ├── request.js             # 网络请求
│   │   └── device.js              # 设备 ID 生成
│   └── pages/
│       ├── login/                 # 登录页面
│       │   ├── login.js
│       │   ├── login.wxml
│       │   ├── login.wxss
│       │   └── login.json
│       └── home/                  # 主页
│           ├── home.js
│           ├── home.wxml
│           ├── home.wxss
│           └── home.json
│
├── backend/                       # 后端服务
│   ├── server.js                  # Express 服务（所有 API）
│   ├── package.json
│   └── data/                      # JSON 数据存储
│       ├── counselors.json        # 辅导员账号
│       └── classes/
│           ├── class_2024/        # 示例班级
│           │   ├── accounts.json
│           │   ├── passcode.json
│           │   └── attendance.json
│           └── class_计算机2511/  # 示例班级
│               ├── accounts.json
│               ├── passcode.json
│               └── attendance.json
│
└── 设备ID位置说明.md              # 设备重置文档
```

---

## 六、API 接口清单

| 接口 | 方法 | 功能 |
|------|------|------|
| `/api/login` | POST | 登录 + 设备绑定 |
| `/api/classes` | GET | 获取班级列表（辅导员） |
| `/api/classes` | POST | 创建班级（辅导员） |
| `/api/classes/:classId` | DELETE | 删除班级（辅导员） |
| `/api/classes/:classId/accounts` | GET | 获取班级账号 |
| `/api/classes/:classId/accounts` | POST | 添加账号（辅导员） |
| `/api/classes/:classId/accounts/:account` | PUT | 编辑账号（辅导员） |
| `/api/classes/:classId/accounts/:account` | DELETE | 删除账号（辅导员） |
| `/api/classes/:classId/passcode` | POST | 发布口令 |
| `/api/classes/:classId/passcode` | DELETE | 清除口令 |
| `/api/classes/:classId/passcode` | GET | 获取口令状态 |
| `/api/classes/:classId/checkin` | POST | 学生签到 |
| `/api/classes/:classId/attendance` | GET | 获取签到名单 |
| `/api/classes/:classId/attendance/:account` | GET | 获取签到状态 |

---

## 七、部署历程

### 1. 纯小程序版本（已废弃）
- 所有数据存储在本地（wx.setStorageSync）
- 无法实现真正的设备绑定
- 各手机数据不互通

### 2. 后端版本（当前）
- 部署方式：阿里云服务器 / 微信云托管
- JSON 文件存储
- 完整的 deviceId 绑定功能

### 3. 部署到阿里云（已测试）
- IP: 8.136.207.96
- 端口: 3000
- 问题：微信小程序不支持 IP 地址作为域名

### 4. 微信云托管（推荐）
- 无需域名备案
- 无需配置服务器域名
- 选择 express.js 模板

---

## 八、部署到微信云托管步骤

1. 登录微信公众平台
2. 云开发 → 云托管
3. 创建服务，选择 express.js 模板
4. 上传 backend 代码
5. 部署成功后获取云托管地址
6. 修改 miniprogram/app.js 中的 baseUrl

---

## 九、设备重置说明

详见 `设备ID位置说明.md`

### 重置位置

- 辅导员：`backend/data/counselors.json`
- 班级学生：`backend/data/classes/class_xxx/accounts.json`

### 字段说明

| 字段 | 说明 |
|------|------|
| `deviceId` | 永久设备绑定，首次登录时自动绑定 |
| `todayDeviceId` | 当日设备绑定，每天 0 点自动清空 |

---

## 十、小程序端配置

### app.js 配置

```javascript
globalData: {
  baseUrl: 'https://xxx.service.cloudbase.cn',  // 部署后填入
  userInfo: null,
  deviceId: ''
}
```

### 权限配置（app.json）

```json
{
  "permission": {
    "scope.userLocation": {
      "desc": "用于签到时验证您的位置"
    }
  },
  "requiredPrivateInfos": ["getLocation"]
}
```

---

## 十一、注意事项

1. 辅导员和学生的 deviceId 分开存储，互不影响
2. 每天 0 点自动重置当日设备绑定
3. 手动重置需编辑 JSON 文件
4. 微信小程序不支持 IP 地址，需要域名或云托管

---

## 十二、后续优化建议

- 增加导出签到记录功能
- 增加签到时间限制
- 增加多班级同时签到
- 使用数据库替代 JSON 文件
