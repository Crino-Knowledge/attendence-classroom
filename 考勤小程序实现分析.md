# 固定班级课堂考勤小程序 - 代码分析

## 一、项目概述

这是一个基于微信小程序（WeChat Mini Program）和 Node.js 后端实现的简易课堂考勤系统。系统采用固定班级模式，适用于中小型课堂场景，老师通过发放临时口令的方式让学生自主签到。

## 二、技术架构

### 前端（微信小程序）

| 文件 | 说明 |
|------|------|
| `miniprogram/app.js` | 小程序全局配置，定义全局变量和服务器地址 |
| `miniprogram/pages/login/` | 登录页面，包含账号密码输入 |
| `miniprogram/pages/home/` | 主页，包含签到功能 |
| `miniprogram/utils/request.js` | 封装的网络请求工具 |
| `miniprogram/utils/device.js` | 设备 ID 生成工具 |

### 后端（Node.js + Express）

| 文件 | 说明 |
|------|------|
| `backend/server.js` | Express 服务器，提供 REST API |
| `backend/data/accounts.json` | 用户账号数据存储 |
| `backend/data/passcodes.json` | 临时口令数据存储 |

## 三、工作流程

### 1. 初始化阶段

```
管理员（老师）操作：
  ├── 编辑 backend/data/accounts.json 添加学生账号
  ├── 通过 API 设置临时口令（调用 /api/set_passcode）
  └── 口令设置后可设置有效期（默认 30 分钟）
```

### 2. 学生登录

```
学生操作：
  ├── 输入账号（学号）和密码
  ├── 点击登录按钮
  └── 系统验证账号密码后跳转至主页

后端处理（/api/login）：
  1. 接收 account、password、deviceId
  2. 从 accounts.json 查找匹配的用户
  3. 验证密码是否正确
  4. 返回登录结果
```

### 3. 签到流程

```
学生操作：
  ├── 输入老师发放的临时口令
  ├── 点击"立即签到"按钮
  └── 系统验证并返回结果

后端处理（/api/check_passcode）：
  1. 验证口令是否正确且未过期
  2. 检查用户是否已绑定设备
  3. 若未绑定则绑定当前设备（一个账号一天只能在一台设备签到）
  4. 记录签到时间到 last_attendance
  5. 返回签到结果
```

## 四、核心实现逻辑

### 4.1 设备绑定机制

系统通过 `deviceId` 实现了"一人一机"防作弊机制：

```javascript
// device.js - 设备 ID 生成逻辑
const generateDeviceId = () => {
  // 优先使用本地缓存的设备 ID
  let deviceId = wx.getStorageSync('device_id');
  if (deviceId) return deviceId;

  // 组合设备信息：品牌|型号|系统|平台 + 随机字符串
  const info = wx.getDeviceInfo();
  const hardware = `${info.brand}|${info.model}|${info.system}|${info.platform}`;
  const randomPart = 'dev_' + Math.random().toString(36).substring(2, 15) + Date.now().toString(36);

  deviceId = hardware + '|' + randomPart;
  wx.setStorageSync('device_id', deviceId);
  return deviceId;
};
```

签到时的设备验证：

```javascript
// backend/server.js - check_passcode 接口
const user = accounts[userIndex];
// 检查是否已在其他设备签到
if (user.today_device && user.today_device !== deviceId) {
  return res.json({ success: false, msg: '该账号已在其他设备签到（当天仅限本机）' });
}

// 第一次签到时绑定设备
if (!user.today_device) {
  user.today_device = deviceId;
  user.last_attendance = new Date().toISOString();
  saveAccounts(accounts);
}
```

### 4.2 自动重置机制

每天午夜 00:00，系统会自动清空所有用户的 `today_device` 字段，实现每日签到重置：

```javascript
// backend/server.js
cron.schedule('0 0 * * *', () => {
  let accounts = loadAccounts();
  accounts.forEach(u => { u.today_device = ''; });
  saveAccounts(accounts);
  console.log('✅ 每天清空 today_device 完成');
});
```

### 4.3 临时口令机制

- 口令由老师通过 API 手动设置
- 支持设置有效期（默认 30 分钟）
- 口令验证失败或过期时拒绝签到

```javascript
// backend/server.js - set_passcode 接口
app.post('/api/set_passcode', (req, res) => {
  const { passcode, duration_min = 30 } = req.body;
  const expiry = new Date(Date.now() + duration_min * 60 * 1000);
  savePasscode({ passcode, expiry: expiry.toISOString() });
  res.json({ success: true, msg: `口令设置成功，有效期 ${duration_min} 分钟` });
});
```

## 五、数据结构

### accounts.json

```json
[
  {
    "account": "2023001",      // 账号（学号）
    "password": "123456",       // 密码
    "today_device": "",        // 当天绑定的设备 ID（每天自动清空）
    "last_attendance": ""      // 最后一次签到时间
  }
]
```

### passcodes.json

```json
{
  "passcode": "ABCD1234",              // 当前有效的临时口令
  "expiry": "2024-01-01T12:30:00.000Z" // 口令过期时间
}
```

## 六、API 接口清单

| 接口 | 方法 | 参数 | 功能 |
|------|------|------|------|
| `/api/login` | POST | account, password, deviceId | 用户登录 |
| `/api/check_passcode` | POST | account, passcode, deviceId | 验证口令并签到 |
| `/api/set_passcode` | POST | passcode, duration_min | 管理员设置临时口令 |

## 七、使用说明

### 老师操作流程

1. 在 `backend/data/accounts.json` 中添加学生账号
2. 启动后端服务：`cd backend && npm install && node server.js`
3. 使用 Postman 或 curl 调用 `/api/set_passcode` 设置临时口令
4. 告知学生临时口令

### 学生操作流程

1. 打开小程序，输入账号密码登录
2. 输入老师发放的临时口令
3. 点击签到完成考勤

## 八、系统特性总结

| 特性 | 实现方式 |
|------|----------|
| 账号管理 | 手动编辑 JSON 文件 |
| 签到验证 | 临时口令 + 设备绑定 |
| 防作弊 | 一个账号一天只能在一台设备签到 |
| 每日重置 | 定时任务自动清空设备绑定 |
| 数据存储 | 本地 JSON 文件 |

这是一个轻量级的课堂考勤解决方案，架构简洁，易于部署和使用，适合固定班级的教学场景。
